package project.finaltoyproject.policy;

public class CreditCardPaymentPolicy implements PaymentPolicy{

    @Override
    public int payment(Card creditCard) {

        switch (creditCard)
        {
            case KOOKMIN:

            case SHINHAN:

            case
        }
    }
}
